services:
  bot-mnamer:
    build:
      context: ./bot-net
      dockerfile: Dockerfile
    container_name: bot-mnamer
    restart: unless-stopped
    env_file:
      - .env
    user: "${PUID:-1000}:${PGID:-1000}"
    volumes:
      - ./appdata:/data
      - ./media:/media
